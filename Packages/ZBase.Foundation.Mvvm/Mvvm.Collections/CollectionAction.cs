namespace ZBase.Foundation.Mvvm.Collections
{
    public enum CollectionAction
    {
        Undefined = 0,
        Add,
        Insert,
        AddRange,
        Remove,
        RemoveAt,
        Replace,
        Clear,
    }
}