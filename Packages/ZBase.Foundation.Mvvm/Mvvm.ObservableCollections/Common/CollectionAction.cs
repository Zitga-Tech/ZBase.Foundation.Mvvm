namespace ZBase.Foundation.Mvvm.ObservableCollections
{
    public enum CollectionAction
    {
        Undefined = 0,
        Add,
        AddRange,
        Insert,
        InsertRange,
        Remove,
        RemoveRange,
        RemoveAt,
        RemoveRangeAt,
        Replace,
        ReplaceAt,
        Clear,
    }
}